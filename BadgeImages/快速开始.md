# 勋章图片导入 - 快速开始指南

## 📋 操作步骤（按顺序执行）

### 步骤 1: 准备图片文件

将你的图片文件放入对应的文件夹：

```bash
# 国家勋章图片（使用中文名称，如：俄罗斯.jpg）
BadgeImages/Countries/俄罗斯.jpg
BadgeImages/Countries/中国.jpg
BadgeImages/Countries/美国.jpg
...

# 省份勋章图片（使用中文名称，如：北京.jpg）
BadgeImages/Provinces/北京.jpg
BadgeImages/Provinces/上海.jpg
BadgeImages/Provinces/广东.jpg
...
```

**提示**: 
- 支持 `.jpg`, `.jpeg`, `.png` 格式
- 文件名使用中文名称即可（如 `俄罗斯.jpg`）
- 脚本会自动识别并映射到对应的资源名称

---

### 步骤 2: 运行导入脚本

打开终端，进入项目根目录，然后执行：

#### 方式一：使用 Shell 脚本（推荐）

```bash
# 1. 进入项目根目录
cd "/Users/kevinx/Documents/Ai Project/Footprint"

# 2. 导入国家勋章图片
./scripts/import_badge_images.sh country

# 3. 导入省份勋章图片
./scripts/import_badge_images.sh province
```

#### 方式二：使用 Python 脚本

```bash
# 1. 进入项目根目录
cd "/Users/kevinx/Documents/Ai Project/Footprint"

# 2. 导入国家勋章图片
python3 scripts/import_badge_images.py country

# 3. 导入省份勋章图片
python3 scripts/import_badge_images.py province
```

---

### 步骤 3: 查看导入结果

脚本运行后会显示：
- ✅ 成功导入的数量
- ⚠️ 跳过（已存在）的数量
- ❌ 错误的数量

然后在 Xcode 中：
1. 打开 `Footprint/Assets.xcassets`
2. 查看 `CountryBadges/` 和 `ProvinceBadges/` 文件夹
3. 确认图片已成功导入

---

## 🎯 完整示例

假设你有以下图片文件：

```
BadgeImages/
├── Countries/
│   ├── 俄罗斯.jpg
│   ├── 中国.jpg
│   └── 美国.jpg
└── Provinces/
    ├── 北京.jpg
    └── 上海.jpg
```

执行命令：

```bash
cd "/Users/kevinx/Documents/Ai Project/Footprint"

# 导入国家图片
./scripts/import_badge_images.sh country

# 导入省份图片
./scripts/import_badge_images.sh province
```

导入后，在 `Assets.xcassets` 中会看到：

```
Assets.xcassets/
├── CountryBadges/
│   ├── CountryBadge_RU.imageset/    (俄罗斯.jpg → RU)
│   ├── CountryBadge_CN.imageset/    (中国.jpg → CN)
│   └── CountryBadge_US.imageset/     (美国.jpg → US)
└── ProvinceBadges/
    ├── ProvinceBadge_北京.imageset/
    └── ProvinceBadge_上海.imageset/
```

---

## ⚠️ 常见问题

### 问题 1: 脚本没有执行权限

**解决**:
```bash
chmod +x scripts/import_badge_images.sh
chmod +x scripts/import_badge_images.py
```

### 问题 2: 提示"源文件夹不存在"

**解决**: 确保已创建文件夹：
```bash
mkdir -p BadgeImages/Countries
mkdir -p BadgeImages/Provinces
```

### 问题 3: 图片导入后 Xcode 中看不到

**解决**: 
1. 在 Xcode 中右键点击 `Assets.xcassets` → "Add Files to..."
2. 或者关闭并重新打开 Xcode 项目

---

## 📝 注意事项

1. **图片命名**: 使用中文名称（如 `俄罗斯.jpg`），脚本会自动映射
2. **批量导入**: 可以一次性放入所有图片，然后运行脚本
3. **重新导入**: 如果图片已存在，脚本会跳过（不会覆盖）
4. **保留原文件**: 原始图片文件不会被删除

---

## 🔍 查看图片名称列表

如果想查看所有需要的图片名称：

```bash
./scripts/generate_badge_list.sh
```

这会生成 `BadgeImages/image_name_list.txt` 文件，包含所有图片名称列表。

