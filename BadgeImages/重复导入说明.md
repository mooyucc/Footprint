# 重复导入图片的处理说明

## 🔄 默认行为：自动跳过已存在的图片

当你多次运行导入脚本时，**已存在的图片会被自动跳过**，不会覆盖。

### 示例

假设你第一次导入了：
- `俄罗斯.jpg` → `CountryBadge_RU.imageset`

第二次运行时，如果 `CountryBadge_RU.imageset` 已存在：
```
  ⚠ 跳过已存在的: CountryBadge_RU
```

**优点**：
- ✅ 安全：不会意外覆盖已导入的图片
- ✅ 快速：只处理新图片
- ✅ 适合增量导入：可以不断添加新图片

---

## 📝 使用场景

### 场景 1：逐步添加新图片（推荐）

```bash
# 第一次：导入 10 个国家图片
./scripts/import_badge_images.sh country
# 输出：成功导入 10 个

# 第二次：添加 5 个新图片，再次运行
./scripts/import_badge_images.sh country
# 输出：跳过 10 个（已存在），成功导入 5 个（新图片）
```

**这是推荐的使用方式**，适合你不断补充图片的场景。

---

### 场景 2：需要更新已存在的图片

如果你修改了某个图片文件（如 `俄罗斯.jpg`），想要更新 Assets 中的图片：

#### 方式一：使用强制覆盖参数（推荐）

```bash
# 强制覆盖所有已存在的图片
./scripts/import_badge_images.sh country --force
# 或
./scripts/import_badge_images.sh country -f
```

这会：
- ✅ 删除旧的 `.imageset` 文件夹
- ✅ 重新导入图片
- ✅ 生成新的 `Contents.json`

#### 方式二：手动删除后重新导入

1. 在 Xcode 中删除对应的 `.imageset` 文件夹
2. 重新运行导入脚本

---

## 🎯 完整示例

### 示例 1：正常增量导入

```bash
# 第一次导入
$ ./scripts/import_badge_images.sh country
✓ 导入成功: CountryBadge_CN
✓ 导入成功: CountryBadge_US
✓ 导入成功: CountryBadge_RU
成功导入: 3 个

# 添加新图片后，第二次导入
$ ./scripts/import_badge_images.sh country
  ⚠ 跳过已存在的: CountryBadge_CN
  ⚠ 跳过已存在的: CountryBadge_US
  ⚠ 跳过已存在的: CountryBadge_RU
✓ 导入成功: CountryBadge_JP
✓ 导入成功: CountryBadge_KR
成功导入: 2 个
跳过（已存在）: 3 个
```

### 示例 2：强制覆盖更新

```bash
# 修改了 俄罗斯.jpg 后，强制更新
$ ./scripts/import_badge_images.sh country --force
  ↻ 覆盖已存在的: CountryBadge_CN
  ↻ 覆盖已存在的: CountryBadge_US
  ↻ 覆盖已存在的: CountryBadge_RU
✓ 导入成功: CountryBadge_CN
✓ 导入成功: CountryBadge_US
✓ 导入成功: CountryBadge_RU
成功导入: 3 个
```

---

## 📊 脚本输出说明

脚本会显示三种状态：

1. **✓ 导入成功**：新图片成功导入
2. **⚠ 跳过已存在的**：图片已存在，跳过（默认行为）
3. **↻ 覆盖已存在的**：强制覆盖模式，更新已存在的图片

---

## 💡 最佳实践

### 推荐工作流程

1. **首次导入**：
   ```bash
   ./scripts/import_badge_images.sh country
   ```

2. **添加新图片后**：
   ```bash
   # 直接运行，脚本会自动跳过已存在的
   ./scripts/import_badge_images.sh country
   ```

3. **更新已存在的图片**：
   ```bash
   # 使用强制覆盖参数
   ./scripts/import_badge_images.sh country --force
   ```

### 注意事项

- ⚠️ **默认跳过**：已存在的图片不会被覆盖，这是安全的行为
- ✅ **增量导入**：适合不断补充新图片的场景
- 🔄 **强制覆盖**：只有在需要更新已存在图片时才使用 `--force`

---

## 🔍 如何判断图片是否已导入？

### 方法 1：查看脚本输出

运行脚本时，会显示：
```
  ⚠ 跳过已存在的: CountryBadge_RU
```

### 方法 2：在 Xcode 中查看

打开 `Assets.xcassets` → `CountryBadges/` 或 `ProvinceBadges/`，查看是否存在对应的 `.imageset` 文件夹。

### 方法 3：查看统计信息

脚本最后会显示：
```
成功导入: 5 个
跳过（已存在）: 10 个
```

---

## ❓ 常见问题

### Q: 我修改了图片文件，为什么 Assets 中没有更新？

**A**: 脚本默认跳过已存在的图片。使用 `--force` 参数强制更新：
```bash
./scripts/import_badge_images.sh country --force
```

### Q: 可以只更新某个特定的图片吗？

**A**: 可以：
1. 在 Xcode 中手动删除对应的 `.imageset` 文件夹
2. 重新运行导入脚本（不使用 `--force`）

### Q: 使用 `--force` 会删除所有图片吗？

**A**: 不会。`--force` 只会覆盖**已存在且匹配的图片**，不会删除其他图片。

---

## 📚 相关命令

```bash
# 正常导入（跳过已存在的）
./scripts/import_badge_images.sh country
./scripts/import_badge_images.sh province

# 强制覆盖（更新已存在的）
./scripts/import_badge_images.sh country --force
./scripts/import_badge_images.sh province -f

# Python 版本也支持
python3 scripts/import_badge_images.py country --force
```

